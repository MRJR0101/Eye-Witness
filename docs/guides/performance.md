# Performance Guide

Benchmark commands:

```powershell
python scripts/benchmark_logging.py
python scripts/benchmark_tracing.py
python scripts/benchmark_metrics.py
```

Tune sampling and exporters to control overhead.
